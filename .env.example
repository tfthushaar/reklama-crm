# Leave both empty to use a local SQLite file (.data/local.db) — nothing to install.
# Set them to use a Turso database (the Vercel Turso integration adds these automatically).
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Random string used to sign login sessions. Required on any shared deployment.
SESSION_SECRET=change-me
