# Leave DATABASE_URL empty to use the embedded local database (stored in .data/pglite).
# Set it to a Postgres connection string (Supabase, InsForge, Neon, RDS...) for a hosted database.
DATABASE_URL=

# Random string used to sign login sessions. Required in production.
SESSION_SECRET=change-me
